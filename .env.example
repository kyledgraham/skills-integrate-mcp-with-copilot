# Firebase Configuration
# Path to your Firebase service account credentials JSON file
# Download this from Firebase Console > Project Settings > Service Accounts
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json

# Optional: Firebase configuration details (used as fallback)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_DATABASE_URL=https://your-project-id.firebaseapp.com
